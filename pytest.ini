[pytest]
DJANGO_SETTINGS_MODULE = serverConfig.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    auth: Authentication tests
    unit: Unit tests
    integration: Integration tests

